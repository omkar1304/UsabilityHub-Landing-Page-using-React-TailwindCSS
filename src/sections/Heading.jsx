import React from "react";

const Heading = () => {
  return (
    <section className="min-w-full h-[40px] bg-blue-200 bg-heading-img bg-contain flex justify-center items-center">
      <p className="text-sm leading-normal px-2 max-xs:text-[0.670rem]">
        📣 Interviews is now in beta! Read more →
      </p>
    </section>
  );
};

export default Heading;
